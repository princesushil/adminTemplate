import { MachineMasterModel } from './machine-master-model';

describe('MachineMasterModel', () => {
  it('should create an instance', () => {
    expect(new MachineMasterModel()).toBeTruthy();
  });
});
