import { CustomerMasterModel } from './customer-master-model';

describe('CustomerMasterModel', () => {
  it('should create an instance', () => {
    expect(new CustomerMasterModel()).toBeTruthy();
  });
});
